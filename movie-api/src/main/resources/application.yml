spring:
  application:
    name: movie-api

  datasource:
    url: jdbc:mysql://localhost:3306/hh99_cinema_sj
    username: root
    password: tnwlssql1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 초기에는 update, 이후 validate로 변경
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace